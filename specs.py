class specs:

    # class attribute
    specs = "specifications"

    # instance attribute
    def __init__(self, width, length, value):
        self.name = name
        self.width = width
        self.length = length
        self.value = value
